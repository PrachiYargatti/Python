# 5. ATM PIN Verification

actual_pin = 1234
attempt = 1
while attempt < 4:
    pin = int(input("enter the PIN number: "))
    if(pin == actual_pin):
        print("\nAccess granted !!")
        break
    else:
        if(attempt < 3):
            print("enter valid pin")
    attempt += 1

if(attempt>3):
    print("\nCard is blocked")
