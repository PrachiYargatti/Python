def convert_str_to_int(num_list):
    new_list=[]
    for i in num_list:
        new_list.append(int(i))
    return new_list
    
n=int(input())
result=[]
for i in range(n):
    num_list=input().split()
    num_list=convert_str_to_int(num_list)
    result.append(max(num_list))
print(result)
